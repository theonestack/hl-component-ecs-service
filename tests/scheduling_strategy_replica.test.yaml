test_metadata:
  type: config
  name: scheduling_strategy_replica
  description: Creates an ECS service with the default (replica) scheduling strategy

task_definition:
  sftp:
    image: nginx
    tag: latest
    privileged: true
    ports:
      - 80
